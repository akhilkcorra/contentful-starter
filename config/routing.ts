/**
 * The slug of the `home` page set in Contentful.
 * Used for redirects/rewrites in the middleware.
 */
export const homeSlug = 'home'

/**
 * The slug of the `search` page set in Contentful.
 * Used for redirects/rewrites in the middleware.
 */
export const searchSlug = 'search'

/**
 * The slug of the `404` page set in Contentful.
 * Used for redirects/rewrites in the middleware.
 */
export const notFoundSlug = '404'

export const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL
